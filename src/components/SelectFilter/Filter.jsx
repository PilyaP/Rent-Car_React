import React from 'react'

export const Filter = () => {
  return (
    <div>
      {' '}
      {/* <select>
        {module.map(item => (
          <option value={item.make}>{item.make}</option>
        ))}
      </select> */}gt
    </div>
  );
}
